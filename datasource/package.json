{
  "name": "datasource",
  "version": "1.0.0",
  "scripts": {
    "start": "node --trace-uncaught datasource.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pusher": "^5.1.2"
  }
}
